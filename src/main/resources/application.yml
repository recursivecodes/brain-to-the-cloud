micronaut:
  application:
    name: bttc
  router:
    static-resources:
      default:
        enabled: true
        mapping: /**
        paths: classpath:public
  security:
    authentication: session
    redirect:
      login-success: /games
      login-failure: /login/authFailed
      forbidden:
        url: /login/auth
      unauthorized:
        url: /login/auth
      logout: /
    intercept-url-map:
      - pattern: /css/*
        http-method: GET
        access:
          - isAnonymous()
      - pattern: /img/**
        http-method: GET
        access:
          - isAnonymous()
      - pattern: /ws/**
        http-method: GET
        access:
          - isAnonymous()
      - pattern: ws**
        http-method: GET
        access:
          - isAnonymous()
      - pattern: /scripts/**
        http-method: GET
        access:
          - isAnonymous()
      - pattern: /bootstrap/**
        http-method: GET
        access:
          - isAnonymous()
    endpoints:
      logout:
        get-allowed: true
datasources:
  default:
    dialect: ORACLE
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    ocid: ${DB_OCID}
    walletPassword: ${DB_WALLET_PASSWORD}
    connection-factory-class-name: oracle.jdbc.pool.OracleDataSource
jpa.default.properties.hibernate.show_sql: false
mqtt:
  client:
    server-uri: ${MQTT_SERVER_URI}
    client-id: bttc-client
    user-name: ${MQTT_USERNAME}
    password: ${MQTT_PASSWORD}
oci.config.profile: DEFAULT
codes:
  recursive:
    xbox-gamertag: ${XBOX_GAMERTAG}
    app-user: ${APP_USER}
    app-password: ${APP_PASSWORD}
    secret-ocid: ocid1.vaultsecret.oc1.phx.amaaaaaaxronvgyajz44fyk2qdzqk2bitrvp5ruqfqsr7o6f5kb56muqewpa
cod:
  sso-token: ${ACTIVISION_TOKEN}
  xsrf-token: ${random.uuid}
  user-agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/74.0.3729.169 Safari/537.36w"
oci.config.profile: DEFAULT